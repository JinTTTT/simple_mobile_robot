# FastSLAM Package

This package starts from first principles.

Stage 1 goal:

- create one clean ROS 2 node
- subscribe to `/odom`
- subscribe to `/scan`
- confirm the basic interface for future FastSLAM work

Run with:

```bash
ros2 run slam_fastslam fastslam_node
```
